checks:
    php:
        code_rating: true
        duplication: true
filter:
    excluded_paths:
        - tests/*
build:
    image: default-bionic
    environment:
        php: 8.1.2
    dependencies:
        before:
            - 'rm -f composer.lock'
    nodes:
        analysis:
            project_setup:
                override: true
            tests:
                override: [php-scrutinizer-run]
